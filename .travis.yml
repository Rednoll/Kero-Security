language: java
jdk:
- openjdk8

before_install:
  - chmod +x gradlew

after_success:
  - bash <(curl -s https://codecov.io/bash)
  - bash <(curl -Ls https://coverage.codacy.com/get.sh)

deploy:
  provider: script
  script: "./gradlew deploy"
  skip_cleanup: true
  dry-run: false
